<?xml version="1.0" encoding="utf-8"?>
<!--
  4 x 2 — eight suggestions, and the size this app started as.

  Kept under the original file name, like the receiver it belongs to, so the widget
  already sitting on a home screen survives an update. See suggest_widget_info_2x2.xml
  for the sizing rules.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minHeight="110dp"
    android:minWidth="250dp"
    android:previewLayout="@layout/widget_preview_4x2"
    android:resizeMode="none"
    android:targetCellHeight="2"
    android:targetCellWidth="4"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
